class InfoScreen {
    // ... Eigenschaften und Methoden für den Infoscreen ...
    isVisible = false;

    draw(ctx) {
        if (this.isVisible) {
            // Zeichnen Sie die Spielanleitung hier
        }
    }
}